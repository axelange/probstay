import { ModulePlaceholder } from "@/components/module-placeholder";

export const metadata = { title: "Utilisateurs — BSTAY PRO" };

export default function Page() {
  return (
    <ModulePlaceholder
      title="Utilisateurs"
      description="Invitations, rôles et permissions."
    />
  );
}
