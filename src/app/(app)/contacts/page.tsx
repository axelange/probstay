import { ModulePlaceholder } from "@/components/module-placeholder";

export const metadata = { title: "Contacts — BSTAY PRO" };

export default function Page() {
  return (
    <ModulePlaceholder
      title="Contacts"
      description="Propriétaires, clients, partenaires et prestataires."
    />
  );
}
