import { ModulePlaceholder } from "@/components/module-placeholder";

export const metadata = { title: "Biens — BSTAY PRO" };

export default function Page() {
  return (
    <ModulePlaceholder
      title="Biens"
      description="Les 52 biens synchronisés depuis APIMO seront listés ici."
    />
  );
}
